extern puts :: fn(s: cstring) i32;

Shape :: enum {
    Circle;
    Square;
}

main :: fn() void {
    shape := Shape.Circle;

    if (shape == Shape.Circle) {
        puts("circle");
    } else if (shape == Shape.Square) {
        puts("square");
    }
}
