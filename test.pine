extern puts :: fn(s: cstring) i32;

main :: fn(args: []string) void {
    for (i: isize = 0; i < cast(isize)args.len; i += 1;) {
        if (i == 0) {
            continue;
        }
        if (i == 2) {
            break;
        }

        puts(args[i].ptr);
    }
}
